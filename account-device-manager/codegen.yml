overwrite: true
schema: 'http://localhost:4000/graphql'
documents: "app/**/*.tsx"
generates:
  graphql/gateway/generated/schema-types.tsx:
    plugins:
      - 'typescript'
      - 'typescript-operations'
    config:
      enumsAsTypes: true
